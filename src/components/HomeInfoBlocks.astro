<section class="home-grid">
  <article class="note-card">
    <h2>Qué es D³</h2>
    <p>
      D³ entiende el dibujo como una forma de observar, conectar y habitar el
      mundo, más allá de la técnica o la perfección.
    </p>
  </article>

  <article class="note-card">
    <h2>Cómo seguirlo</h2>
    <p>
      La actividad viva del proyecto sucede en Instagram. Aquí en la web vive su
      memoria, su mapa y sus recursos.
    </p>
  </article>

  <article class="note-card">
    <h2>Qué puedes explorar</h2>
    <p>
      El mapa de sesiones pasadas, ejercicios para soltar la mano, Tinta y las
      formas de contacto o apoyo al proyecto.
    </p>
  </article>
</section>

<style>
  .home-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 1.2rem;
    margin: 0 0 3rem;
    align-items: start;
  }

  .note-card {
  background: rgba(255, 255, 255, 0.79);
  backdrop-filter: blur(4px);
  -webkit-backdrop-filter: blur(4px);
  border: 2px solid #111;
  border-radius: 24px;
  padding: 1.25rem;
  box-shadow: 4px 4px 0 #111;
  min-height: 170px;
}

  .note-card h2 {
    margin: 0 0 0.7rem;
    font-size: 1.25rem;
  }

  .note-card p {
    margin: 0;
    line-height: 1.7;
  }

  @media (max-width: 900px) {
    .home-grid {
      grid-template-columns: 1fr;
    }

    .note-card {
      min-height: auto;
    }
  }
</style>