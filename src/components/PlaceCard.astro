---
interface Props {
  place: {
    id: number;
    slug: string;
    name: string;
    type: string;
    date: string;
    address: string;
    image: string;
    instagramUrl: string;
    mapsUrl: string;
    excerpt: string;
  };
}

const { place } = Astro.props;
const formattedDate = new Date(place.date).toLocaleDateString("es-MX", {
  year: "numeric",
  month: "long",
  day: "numeric"
});
---

<article class="place-card paper-card">
  <a href={`/mapa-d3/${place.slug}`} class="place-image-link">
    <img src={place.image} alt={place.name} class="place-image" />
  </a>

  <div class="place-content">
    <p class="place-type">{place.type}</p>
    <h3 class="place-title">
      <a href={`/mapa-d3/${place.slug}`}>{place.name}</a>
    </h3>
    <p class="place-date">{formattedDate}</p>
    <p class="place-address">{place.address}</p>
    <p class="place-excerpt">{place.excerpt}</p>

    <div class="place-actions">
      <a href={`/mapa-d3/${place.slug}`} class="btn-primary">Ver lugar</a>
      <a href={place.instagramUrl} target="_blank" rel="noopener noreferrer" class="btn-secondary">
        Ver post
      </a>
    </div>
  </div>
</article>

<style>
  .place-card {
    display: grid;
    grid-template-columns: 220px 1fr;
    gap: 1.25rem;
    align-items: start;
  }

  .place-image-link {
    display: block;
  }

  .place-image {
    width: 100%;
    aspect-ratio: 1 / 1;
    object-fit: cover;
    border-radius: 3px;
    border: 1.5px solid rgba(17, 17, 17, 0.82);
    display: block;
    box-shadow: 3px 3px 0 #11111124;
  }

  .place-content {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .place-type {
    margin: 0;
    font-family: var(--font-accent);
    font-size: 0.78rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: #d9485f;
    font-weight: 700;
  }

  .place-title {
    margin: 0;
    font-size: 1.6rem;
    line-height: 1.06;
    max-width: 16ch;
  }

  .place-title a {
    color: inherit;
    text-decoration: none;
  }

  .place-date,
  .place-address,
  .place-excerpt {
    margin: 0;
    line-height: 1.65;
    color: var(--ink-soft);
  }

  .place-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin-top: 0.55rem;
  }

  .btn-primary,
  .btn-secondary {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.78rem 1rem;
    border: 1.5px solid rgba(17, 17, 17, 0.82);
    border-radius: 3px;
    text-decoration: none;
    font-weight: 700;
    background: rgba(255, 255, 255, 0.74);
    color: #111;
    box-shadow: 4px 4px 0 #11111140;
    transition: transform 140ms ease, box-shadow 140ms ease;
  }

  .btn-primary:hover,
  .btn-secondary:hover {
    transform: translateY(-1px);
    box-shadow: 5px 5px 0 #11111133;
  }

  @media (max-width: 720px) {
    .place-card {
      grid-template-columns: 1fr;
    }

    .place-image {
      aspect-ratio: 16 / 10;
    }

    .place-title {
      max-width: none;
    }
  }
</style>