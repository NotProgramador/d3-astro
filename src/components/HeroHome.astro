<section class="hero">
  <div class="hero-copy">
    <p class="eyebrow">archivo · dibujo · encuentro</p>
    <h1>D³ — Domingos de Dibujar</h1>
    <p class="intro">
      Un proyecto de encuentros de dibujo para observar, explorar y compartir
      sin miedo al trazo.
    </p>

    <div class="hero-actions">
      <a href="/mapa-d3" class="btn-primary">Ver mapa D³</a>
      <a
        href="https://instagram.com/"
        target="_blank"
        rel="noopener noreferrer"
        class="btn-secondary"
      >
        Ir a Instagram
      </a>
    </div>
  </div>
</section>

<style>
  .hero {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
    min-height: 58vh;
    padding: 2rem 0 2.75rem;
    align-items: center;
  }

  .hero-copy {
    max-width: 780px;
  }

  .eyebrow {
    margin: 0 0 0.8rem;
    font-family: var(--font-accent);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    font-size: 0.78rem;
    font-weight: 700;
    color: #d9485f;
  }

  h1 {
    margin: 0 0 1rem;
    font-size: clamp(2.9rem, 8vw, 5.7rem);
    line-height: 0.96;
    letter-spacing: -0.035em;
    max-width: 9.5ch;
  }

  .intro {
    margin: 0;
    max-width: 34ch;
    font-size: 1.08rem;
    line-height: 1.78;
    color: var(--ink-soft);
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.85rem;
    margin-top: 1.55rem;
  }

  .btn-primary,
  .btn-secondary {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    text-decoration: none;
    font-weight: 700;
    color: #111;
    background: rgba(255, 255, 255, 0.74);
    backdrop-filter: blur(4px);
    -webkit-backdrop-filter: blur(4px);
    border: 1.5px solid rgba(17, 17, 17, 0.82);
    border-radius: 3px;
    padding: 0.9rem 1.15rem;
    box-shadow: 4px 4px 0 #11111140;
    transition: transform 140ms ease, box-shadow 140ms ease;
  }

  .btn-primary:hover,
  .btn-secondary:hover {
    transform: translateY(-1px);
    box-shadow: 5px 5px 0 #11111133;
  }

  @media (max-width: 900px) {
    .hero {
      min-height: auto;
      padding-top: 1rem;
    }

    .intro {
      max-width: 36ch;
    }
  }
</style>